/* =====================================================
   Bolig & ansøgning
   Page-specific styling for bolig-ansoegning.html
===================================================== */
.housing-hero-card {
  overflow: hidden;
}

.housing-highlight-list {
  display: grid;
  gap: 0.85rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.housing-highlight-list li {
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  color: var(--color-muted);
}

.housing-highlight-list i {
  flex: 0 0 auto;
  display: inline-grid;
  place-items: center;
  width: 2.15rem;
  height: 2.15rem;
  color: var(--color-primary);
  background-color: var(--color-primary-light);
  border-radius: 999px;
}

.housing-type-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: var(--grid-gap);
}

.housing-type-card {
  display: flex;
  flex-direction: column;
  min-height: 100%;
  padding: clamp(1.25rem, 2.6vw, 1.75rem);
  background-color: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-sm);
}

.housing-type-card-featured {
  background: linear-gradient(135deg, #ffffff, var(--color-secondary-soft));
  border-color: rgba(36, 71, 52, 0.22);
}

.housing-type-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 1rem;
}

.housing-type-icon {
  display: inline-grid;
  place-items: center;
  width: 3.2rem;
  height: 3.2rem;
  color: var(--color-primary);
  background-color: var(--color-primary-light);
  border-radius: 999px;
  font-size: 1.35rem;
}

.housing-badge {
  display: inline-flex;
  align-items: center;
  min-height: 2rem;
  padding: 0.35rem 0.75rem;
  color: var(--color-primary-dark);
  background-color: var(--color-accent-soft);
  border: 1px solid rgba(199, 137, 85, 0.26);
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 800;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  white-space: nowrap;
}

.housing-type-card h3 {
  margin-bottom: 0.65rem;
  font-family: var(--font-heading);
  font-size: clamp(1.35rem, 2vw, 1.7rem);
}

.housing-type-card p {
  color: var(--color-muted);
}

.housing-meta-list {
  display: grid;
  gap: 0.65rem;
  margin: auto 0 0;
  padding: 1rem 0 0;
  list-style: none;
  border-top: 1px solid var(--color-border);
  color: var(--color-muted);
  font-size: 0.94rem;
}

.housing-meta-list strong {
  color: var(--color-primary-dark);
}

.housing-price-layout {
  align-items: center;
}

.application-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(18rem, 0.42fr);
  gap: clamp(2rem, 5vw, 4rem);
  align-items: start;
}

.process-list {
  display: grid;
  gap: 1rem;
}

.process-step {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 1rem;
  padding: clamp(1.1rem, 2.4vw, 1.45rem);
  background-color: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}

.process-number {
  display: inline-grid;
  place-items: center;
  width: 2.75rem;
  height: 2.75rem;
  color: #ffffff;
  background-color: var(--color-primary);
  border-radius: 999px;
  font-weight: 850;
}

.process-step h3 {
  margin-bottom: 0.35rem;
  font-size: 1.08rem;
  font-weight: 800;
}

.process-step p {
  margin-bottom: 0;
  color: var(--color-muted);
}

.sticky-apply-card {
  position: sticky;
  top: 7rem;
  padding: clamp(1.25rem, 3vw, 1.75rem);
  background: linear-gradient(135deg, #ffffff, var(--color-primary-light));
  border: 1px solid rgba(36, 71, 52, 0.22);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-md);
}

.sticky-apply-card h3 {
  margin-bottom: 0.6rem;
  font-family: var(--font-heading);
  font-size: clamp(1.45rem, 2.2vw, 1.85rem);
}

.sticky-apply-card p {
  color: var(--color-muted);
}

.apply-link-list {
  display: grid;
  gap: 0.75rem;
  margin-top: 1rem;
}

.apply-link-list .btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

.faq-accordion .accordion-item {
  background-color: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  overflow: hidden;
  box-shadow: var(--shadow-sm);
}

.faq-accordion .accordion-item + .accordion-item {
  margin-top: 0.75rem;
}

.faq-accordion .accordion-button {
  color: var(--color-primary-dark);
  background-color: var(--color-card);
  font-weight: 800;
}

.faq-accordion .accordion-button:not(.collapsed) {
  color: var(--color-primary-dark);
  background-color: var(--color-primary-light);
  box-shadow: none;
}

.faq-accordion .accordion-button:focus {
  box-shadow: 0 0 0 0.25rem rgba(199, 137, 85, 0.24);
}

.move-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: var(--grid-gap);
}

.move-card {
  height: 100%;
  padding: clamp(1.2rem, 2.5vw, 1.55rem);
  background-color: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}

.move-card-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 2.85rem;
  height: 2.85rem;
  margin-bottom: 1rem;
  color: var(--color-primary-dark);
  background-color: var(--color-primary-light);
  border-radius: 999px;
  font-size: 1.2rem;
}

.move-card h3 {
  margin-bottom: 0.45rem;
  font-size: 1.12rem;
  font-weight: 800;
}

.move-card p {
  color: var(--color-muted);
}

@media (max-width: 991.98px) {
  .housing-type-grid,
  .application-layout,
  .move-grid {
    grid-template-columns: 1fr;
  }

  .sticky-apply-card {
    position: static;
  }
}

@media (max-width: 575.98px) {
  .housing-type-top,
  .process-step {
    grid-template-columns: 1fr;
  }

  .housing-type-top {
    align-items: flex-start;
    flex-direction: column;
  }
}
